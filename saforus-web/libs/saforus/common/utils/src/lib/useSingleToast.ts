// Move show toast to shared component
import { showToast as toast } from '@web-workspace/shared/components/widgets/toast';

// Show toast is already deinfe in the share toast component
export const showToast = toast;
